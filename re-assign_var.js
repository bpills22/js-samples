let changeMe = true;

changeMe = false;
console.log(changeMe);

//don't use let again when you want to change the variable

const entree = 'Enchiladas';
console.log(entree);

entree = 'Tacos';
//this will throw an error

const testing;
//also throws an error because no value has been assigned
